<%= simple_form_for(@new_word_adj_form) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :adj_type %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
