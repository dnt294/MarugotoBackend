<%= simple_form_for(@grammar) do |f| %>
    <div class="form-inputs">
        <%= f.input :title, as: :text, input_html: { rows: 2 }, class: 'multilines ' %>
        <%= f.input :explanation %>
        <%= f.input :note, as: :ckeditor %>
        <%= f.association :lesson, label_method: :select_label, value_method: :id, collection: Lesson.marugoto_books %>
    </div>
    <div class="ui horizontal divider">
        Ví dụ
    </div>
    <div class="examples">
        <%= f.simple_fields_for :examples do |example| %>
            <%= render 'example_fields', f: example %>
        <% end %>
        <%= link_to_add_association 'Thêm ví dụ', f, :examples, class: 'ui green button' %>
    </div>
    <div class="ui divider"></div>
    <div class="form-actions">
        <%= f.button :submit %>
        <%= link_to 'Back', grammars_path, class: 'ui button negative' %>
    </div>
<% end %>